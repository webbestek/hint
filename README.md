# hint — beginner-friendly terminal hints

Run `hint` to see colorful, plain-English tips with exact commands and how to use them.
Use `hint i` for an interactive picker (fzf if installed).

- Beginner view is the default.
- Safe, fast, pure-Bash runtime.
- Works from any path (dynamic root resolution).

Dev install:
```bash
git clone git@github.com:webbestek/hint.git ~/Projects/hint
~/Projects/hint/scripts/dev-install.sh
source ~/.bashrc
```
